{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://bomsquad.dev/schemas/implementation_chip.schema.json",
  "title": "implementation_chip",
  "description": "One row of the `implementation_chip` table. Keys and order follow schemas/schema.sql; NULL columns are omitted, never written as null (data-model.md §4.3).",
  "type": "object",
  "additionalProperties": false,
  "required": ["implementation_id", "chip_id"],
  "properties": {
    "implementation_id": {
      "$ref": "common.schema.json#/$defs/slug"
    },
    "chip_id": {
      "$ref": "common.schema.json#/$defs/slug"
    }
  }
}
