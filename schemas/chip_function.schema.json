{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://bomsquad.dev/schemas/chip_function.schema.json",
  "title": "chip_function",
  "description": "One row of the `chip_function` table. Keys and order follow schemas/schema.sql; NULL columns are omitted, never written as null (data-model.md §4.3).",
  "type": "object",
  "additionalProperties": false,
  "required": ["function_id", "label", "description", "prospector_band"],
  "properties": {
    "function_id": {
      "$ref": "common.schema.json#/$defs/slug"
    },
    "label": {
      "$ref": "common.schema.json#/$defs/text"
    },
    "description": {
      "$ref": "common.schema.json#/$defs/text"
    },
    "prospector_band": {
      "type": "string",
      "enum": ["hard", "medium", "soft"],
      "description": "Domain difficulty band, not a score. Weights live in pipeline/config."
    }
  }
}
