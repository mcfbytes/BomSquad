{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://bomsquad.dev/schemas/fpga_platform.schema.json",
  "title": "fpga_platform",
  "description": "One row of the `fpga_platform` table. Keys and order follow schemas/schema.sql; NULL columns are omitted, never written as null (data-model.md §4.3).",
  "type": "object",
  "additionalProperties": false,
  "required": ["platform_id", "label"],
  "properties": {
    "platform_id": {
      "$ref": "common.schema.json#/$defs/slug"
    },
    "label": {
      "$ref": "common.schema.json#/$defs/text"
    },
    "notes": {
      "$ref": "common.schema.json#/$defs/text"
    }
  }
}
