{
  "implementation": [
    {
      "implementation_id": "mister-arcade-superbreakout",
      "name": "Super Breakout (MiSTer)",
      "kind_id": "fpga_hdl",
      "project_id": "mister-devel",
      "repo_url": "https://github.com/MiSTer-devel/Arcade-SuperBreakout_MiSTer",
      "hdl_language_id": "mixed",
      "last_reviewed": "2026-07-28",
      "notes": "Source availability (T5.4): open -- the repository carries 21 HDL source files (2 systemverilog, 2 verilog, 17 vhdl) alongside 10 prebuilt .rbf binaries. There is no open_source column in the delivered schema (data-model.md §1.9 deleted the proposed core table together with its flags); the presence of hdl_language_id and of implementation_path rows is how that fact is stated here. Machine mapping: 1 MRA files in the repository's releases/ directory carry a <setname>; 1 resolved exactly to a MAME 0.288 machine and 0 through MAME's own cloneof relation to the parent set that survives extract/machine.json's parents-only filter, giving 1 distinct machine_id rows. license_id omitted: the two license statements in the repository disagree. https://github.com/MiSTer-devel/Arcade-SuperBreakout_MiSTer/blob/master/LICENSE is the verbatim GNU GPL version 3 text, but the grant in https://github.com/MiSTer-devel/Arcade-SuperBreakout_MiSTer/blob/master/Arcade-SuperBreakout.sv offers the work under GPL version 2 or later. Either reading is arguable and neither is a fact the repository states, so no SPDX id is asserted. hdl_language_id: no single HDL family reaches 90% of the 21 source files (2 systemverilog, 2 verilog, 17 vhdl), excluding the shared framework directory. accuracy_id omitted: the repository makes no timing-fidelity claim this build could verify."
    }
  ],
  "implementation_dependency": [
    {
      "consumer_id": "mister-arcade-superbreakout",
      "provider_id": "t65",
      "note": "T65 6502 core vendored at rtl/T65/T65.vhd"
    }
  ],
  "implementation_machine": [
    {
      "implementation_id": "mister-arcade-superbreakout",
      "machine_id": "sbrkout"
    }
  ],
  "implementation_path": [
    {
      "implementation_id": "mister-arcade-superbreakout",
      "path": "Arcade-SuperBreakout.sv",
      "is_top": 1
    },
    {
      "implementation_id": "mister-arcade-superbreakout",
      "path": "rtl"
    }
  ],
  "implementation_platform": [
    {
      "implementation_id": "mister-arcade-superbreakout",
      "platform_id": "mister"
    }
  ]
}
