# scripts

Maintainer and operations scripts: Azure Static Web Apps provisioning, GitHub release workflows, schema code generation, and data archive building. Bash and shell helpers for local development and CI automation.

**Edit policy:** CODE. Reviewed via PR. Keep scripts deterministic and well-documented; avoid hardcoding credentials (use environment variables). These run in CI and local maintainer environments.
